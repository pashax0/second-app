{
  "name": "daily-drop-shop",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "mobile": "pnpm --filter mobile start",
    "build:mobile": "pnpm --filter mobile build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "seed:storage": "bash scripts/seed-storage.sh"
  },
  "devDependencies": {
    "supabase": "^2.77.0"
  }
}
